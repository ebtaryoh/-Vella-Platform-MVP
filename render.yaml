databases:
  - name: vella-db
    databaseName: vella
    user: vella
    plan: free

services:
  - type: web
    name: vella-api
    env: node
    plan: free
    buildCommand: npm install --include=dev && npx prisma generate --schema=apps/api/prisma/schema.prisma && npm run build -w api
    startCommand: cd apps/api && npx prisma db push && npm run start:prod
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vella-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 3000
      - key: NODE_VERSION
        value: 20
